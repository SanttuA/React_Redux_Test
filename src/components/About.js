import React from "react";

function About(props){
    return(
        <main>
            <h1>About Page</h1>
            <p>This is the about page. This is the about page. This is the about page. This is the about page. This is the about page.</p>
        </main>
    );
}

export default About;